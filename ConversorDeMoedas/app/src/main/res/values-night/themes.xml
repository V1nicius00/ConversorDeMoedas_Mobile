<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ConversorDeMoedas" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Cores para o tema escuro -->
        <item name="android:windowBackground">@color/dm_corDeFundo</item>
        <item name="android:colorBackground">@color/dm_corDeFundo</item>
        <item name="android:textColorPrimary">@color/dm_textoPrimario</item>
        <item name="android:textColorSecondary">@color/dm_textoSecundario</item>
        <item name="colorPrimary">@color/dm_corDePrimeiroPlano</item>
    </style>

    <style name="BACKGROUND">
        <item name="android:background">@color/dm_corDeFundo</item>
    </style>

    <style name="BOTAO">
        <item name="android:background">@drawable/btn_dark</item>
    </style>

    <style name="TEXT">
        <item name="android:textColor">@color/dm_textoPrimario</item>
    </style>

</resources>